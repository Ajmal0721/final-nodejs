<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Articles</title>
    <style>
        /* General Styles */
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f4f4f4;
}

/* Container Styles */
.container {
    width: 80%;
    margin: 0 auto;
    padding: 20px;
    background-color: #fff;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    border-radius: 8px;
    margin-top: 20px;
}

/* Article List Styles */
.article-list {
    list-style-type: none;
    padding: 0;
}

.article-item {
    margin-bottom: 20px;
    padding: 15px;
    border-bottom: 1px solid #ddd;
}

.article-item h2 {
    margin-top: 0;
    color: #333;
}

.article-item p {
    margin: 5px 0;
    color: #666;
}

.article-item a {
    text-decoration: none;
    color: #0066cc;
}

.article-item a:hover {
    text-decoration: underline;
}

/* Article Actions Styles */
.article-actions {
    margin-top: 10px;
}

.edit-btn {
    background-color: #ffcc00;
    padding: 5px 10px;
    border: none;
    border-radius: 5px;
    color: #333;
    text-decoration: none;
    font-weight: bold;
}

.edit-btn:hover {
    background-color: #ffbb00;
}

.delete-form {
    display: inline;
}

.delete-btn {
    background-color: #ff6666;
    padding: 5px 10px;
    border: none;
    border-radius: 5px;
    color: #fff;
    font-weight: bold;
    cursor: pointer;
}

.delete-btn:hover {
    background-color: #ff4444;
}

/* No Articles Message */
p {
    font-size: 18px;
    color: #333;
}

    </style>
</head>
<body>

    <!-- Include the navbar -->
    <%- include('navbar') %>

    <div class="container">
        <h1>My Articles</h1>

        <!-- Check if the user has any articles -->
        <% if (articles && articles.length > 0) { %>
            <ul class="article-list">
                <!-- Loop through each article and display it -->
                <% articles.forEach(article => { %>
                    <li class="article-item">
                        <h2><%= article.title %></h2>
                        <p><%= article.content.substring(0, 100) %>...</p>
                        <a href="/article/<%= article._id %>">Read More</a>
                        <!-- Option to edit or delete the article -->
                        <div class="article-actions">
                            <a href="/article/<%= article._id %>/edit" class="edit-btn">Edit</a>
                            <form action="/article/<%= article._id %>/delete" method="POST" class="delete-form">
                                <button type="submit" class="delete-btn">Delete</button>
                            </form>
                        </div>
                    </li>
                <% }); %>
            </ul>
        <% } else { %>
            <p>You haven't written any articles yet.</p>
        <% } %>
    </div>

</body>
</html>
